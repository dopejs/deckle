{
  "name": "@dopejs/deckle-platform-probe",
  "version": "0.4.0",
  "private": true,
  "description": "M0 feasibility probe: HTML-in-Canvas capability detection and signed evidence manifests. Probe code is disposable and is not a production API.",
  "license": "Apache-2.0",
  "homepage": "https://deckle.dopejs.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dopejs/deckle.git",
    "directory": "apps/platform-probe"
  },
  "bugs": {
    "url": "https://github.com/dopejs/deckle/issues"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "node ../../scripts/clean-dist.mjs && tsc -p tsconfig.build.json",
    "test": "vitest run src",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@dopejs/deckle-artifact": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
