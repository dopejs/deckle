{
  "name": "@dopejs/canvas-spatial",
  "version": "0.0.0",
  "private": true,
  "description": "Spatial indexes and the naive differential oracle for dope-canvas visibility queries.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run src",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@dopejs/canvas-protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.0.18"
  }
}
