{
  "name": "@dopejs/canvas-security",
  "version": "0.0.0",
  "private": true,
  "description": "Sanitization, resource policies, capabilities, and quotas for untrusted dope-canvas artifacts.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run src",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.0.18"
  }
}
